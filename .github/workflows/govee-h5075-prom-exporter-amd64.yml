name: govee-h5075-prom-exporter-amd64
permissions:
  contents: read

on:
  workflow_dispatch:
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - 'README.md'
      - 'OPENMETEO_INTEGRATION.md'
      - 'LICENSE.md'
      - '.gitignore'
      - '.github/workflows/*'
      - 'docker-compose.yaml'
      - 'mock_server.py'
  push:
    branches:
      - "main"
    paths-ignore:
      - 'README.md'
      - 'OPENMETEO_INTEGRATION.md'
      - 'LICENSE.md'
      - '.gitignore'
      - '.github/workflows/*'
      - 'docker-compose.yaml'
      - 'mock_server.py'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Extract Go version from Dockerfile
        id: go-version
        run: echo "version=$(grep -oP '(?<=FROM golang:)\d+\.\d+(\.\d+)?' docker/Dockerfile | head -1)" >> $GITHUB_OUTPUT
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ steps.go-version.outputs.version }}
          cache: true
      - name: Run tests
        run: go test -v ./...

  build-and-push:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Extract Go version from Dockerfile
        id: go-version
        run: echo "version=$(grep -oP '(?<=FROM golang:)\d+\.\d+(\.\d+)?' docker/Dockerfile | head -1)" >> $GITHUB_OUTPUT

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ steps.go-version.outputs.version }}
          cache: true
      
      - name: Build AMD64 binary with native Go cross-compilation
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
          go build -trimpath -ldflags="-w -s" -o govee_exporter ./src

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Build frontend assets
        run: node build-css.js && node build-js.js && node build-sw.js

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      
      - name: Login to DockerHub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Build and Push Docker image
        uses: docker/build-push-action@v7
        with:
          push: true
          context: .
          file: ./docker/Dockerfile.prebuilt
          platforms: linux/amd64
          cache-from: type=gha,scope=amd64
          cache-to: type=gha,mode=max,scope=amd64
          provenance: true
          sbom: true
          tags: |
            ${{ secrets.DOCKERHUB_SLUG }}/govee-h5075-prom-exporter:amd64-latest
            ${{ secrets.DOCKERHUB_SLUG }}/govee-h5075-prom-exporter:amd64-${{github.run_number}}
